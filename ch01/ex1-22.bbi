print  a       -- a 값을 출력
println  a     -- a 값을 출력하고 줄을 바꾼다
print a, b, c  -- 여러 인수를 지정할 수 있다
print "a=", a  -- 문자열 리터럴도 이용할 수 있다
a = input()    -- 수치 입력을 한다
a = toint(b)   -- 수치 b의 소숫점 이하를 0으로 한 값을 넣는다
