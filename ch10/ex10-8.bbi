10.7.2 break에 의한 실행 종료 처리

while a < b
  ...
  break ? a == 10  // (1) break_Flg를 참으로 하고 중단을 실행한 후 거짓으로 되돌린다
  ...              // 이때는 (2)(3)은 만나지 않는다
  while c < d
    ...
    break ? c == 20  // (2) break_Flg를 참으로 하고 중단을 실행한 후 거짓으로 되돌린다
    ...
  end
  ...
  break ? a == 30  // (3) break_Flg를 참으로 하고 중단을 실행한 후 거짓으로 되돌린다
  ...
end
