12.1 스테이트먼트와 내장 함수 준비

a = input()    -- 수치를 입력받는다
print a        -- a 값을 출력한다
print "a=", a  -- 문자열 리터럴도 저장할 수 있다
print a, b, c  -- 여러 인수를 지정할 수 있다
println a      -- a 값을 출력한 다음 행을 바꾼다
a = toint(b)   -- 수치 b의 소수점 아래를 0으로 만든 값을 a에 넣는다
